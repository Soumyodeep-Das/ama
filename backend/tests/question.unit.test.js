const Question = require('../models/Question');

describe('Question Model Unit Tests', () => {
  it('should be defined', () => {
    expect(Question).toBeDefined();
  });
  // Add more unit tests here for model logic
});
